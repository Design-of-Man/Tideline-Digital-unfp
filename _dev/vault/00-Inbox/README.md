# Inbox

Manual capture. Anything that arrives before the intake Routine exists goes
here as a note, and gets filed to a client later.

The intake Routine is blocked until the separate agency email address exists —
sweeping `nick@firstrehabnpb.com` would mean an automated process reading
clinic mail on a schedule, which is not worth the convenience.
