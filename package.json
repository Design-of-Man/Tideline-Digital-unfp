{
  "name": "design-of-man",
  "private": true,
  "description": "designofman.com \u2014 a static site with one serverless function.",
  "scripts": {
    "pages": "python3 scripts/pages.py && for p in work services studio process pricing contact case 404 local insights; do python3 \"scripts/build_$p.py\"; done && python3 scripts/sync_index.py && python3 scripts/build_meta.py",
    "verify": "./scrollcraft/verify/run-all.sh"
  },
  "devDependencies": {
    "playwright-core": "^1.62.1"
  }
}
